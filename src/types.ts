export interface DataItem {
  alerts: number;
  avgGain: number;
  name: string;
  rank: number;
  streaks: number;
  trades: number;
  tradingStyle: string;
  trophies: string;
  xScore: number;
}

export interface SummaryItem {
  title: string;
  value: number;
  name: string;
}
